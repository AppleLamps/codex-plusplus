{
  "name": "@codex-plusplus/runtime",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "dist/main.js",
  "description": "In-app runtime: discovers tweaks, injects settings UI, hot-reloads.",
  "scripts": {
    "build": "node ./scripts/clean-dist.mjs && tsc -p tsconfig.json && node ./scripts/bundle-renderer.mjs",
    "test": "node --test --import tsx \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@codex-plusplus/sdk": "*",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "electron": "^41.3.0",
    "esbuild": "^0.28.0",
    "happy-dom": "^20.9.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
